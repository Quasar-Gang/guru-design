# guru-core origin, absolute, without /v1 and without a trailing slash.
# Leave both empty to render the built-in demonstration dataset.
GURU_API_BASE_URL=
# Bearer token from POST /v1/auth/google. Server-side only: it must never carry
# a NEXT_PUBLIC_ prefix, because that would ship it to the browser.
GURU_API_TOKEN=
